[ldap]
    server = ldap://localhost
    accountBase = ou=people,o=myOrg
    groupBase = ou=groups,o=myOrg
    referral = follow
    accountPattern = (uid=${username})
    groupPattern = (cn=${groupname})
    accountFullName = cn
